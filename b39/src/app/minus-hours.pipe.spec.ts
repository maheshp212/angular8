import { MinusHoursPipe } from './minus-hours.pipe';

describe('MinusHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new MinusHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
