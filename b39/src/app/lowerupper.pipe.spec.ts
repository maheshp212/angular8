import { LowerupperPipe } from './lowerupper.pipe';

describe('LowerupperPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerupperPipe();
    expect(pipe).toBeTruthy();
  });
});
