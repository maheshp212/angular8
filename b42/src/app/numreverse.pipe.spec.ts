import { NumreversePipe } from './numreverse.pipe';

describe('NumreversePipe', () => {
  it('create an instance', () => {
    const pipe = new NumreversePipe();
    expect(pipe).toBeTruthy();
  });
});
